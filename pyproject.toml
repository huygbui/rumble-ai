[project]
name = "rumble-ai"
version = "0.1.0"
description = "Self-hosted voice pipeline (STT -> LLM -> TTS) served on Modal"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "modal>=1.5.0",
    "httpx>=0.28",
    "fastapi>=0.135",
    "uvicorn>=0.34",
    "pydantic>=2",
]
