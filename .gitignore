# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated audio (client.py + demo clips both write to ./out)
*.wav
*.mp3
out/*
# keep the demo-clip manifest tracked; the audio itself stays ignored
!out/MANIFEST.txt
# required runtime asset (TTS clone anchor), not scratch audio
!assets/voice_anchor.wav

# macOS
.DS_Store
